precision mediump float;
varying vec2 v_tex_coord;

uniform sampler2D uSampler;


void main() {
    //gl_FragColor = vec4(.6, .3, 1.0 * v_pos.z, 1);
    gl_FragColor = texture2D(uSampler, vec2(v_tex_coord.s, v_tex_coord.t));
}
